;Input:
;ds:dx -> something
;Output:
;es:ax -> another
;idea -> tips with mov word <variable name>
@@:

    mov ax, cx
    mov ax, 12
    mov ax, 12 12
    mov ax, 0x123
    mov ax, $ad
    mov ax, 0Fh
    add ax, 123
    mov word [pBuffer], 
db 


